<%= render "starboard", board: @board %>
<nav class="d-inline-flex mb-3">
    <div class="mx-2 mt-3">
      <h2><%= @board.title %></h2>
    </div>

<!-- Board archived  -->
    <div class="starred-board">
      <a href="" class="btn btn-lg mt-1 btn-bh" data-toggle="modal" data-target="#starBoard">
        <i class="far fa-star"></i>
      </a>
    </div>

<!-- Board Visibility  -->
    <div>
      <button type="button" class="btn btn-lg btn-light ml-2 mt-2 btn-bh">
        <%= @board.visibility %>
      </button>
    </div>

<!-- Board member list  -->
    <div class="dropdown">
      <button type="button" class="btn btn-lg btn-light ml-2 mt-2 btn-bh dropdown-toggle" id="dropdownMenuOffset"
            data-toggle="dropdown" aria-haspopup="true" aria-expanded="false" data-offset="10,20">
            Member
       </button>
       <ul class="dropdown-menu" aria-labelledby="dropdownMenuOffset">
         <% @board.users.each do |member| %>
         <li class="dropdown-item"><%= member.email %></li>
         <% end %>
       </ul>
    </div>

<!-- Form for Invitation to other users -->
    <div class="dropdown">
      <button class="btn btn-lg btn-bh ml-2 mt-2 btn-light dropdown-toggle" type="button" id="dropdownMenu2" data-toggle="dropdown"
            aria-haspopup="true" aria-expanded="false">
            Invite 
      </button>
      <div class="dropdown-menu p-2">
        <%=  form_with scope: :search, url: searchuser_board_path do |f| %>
        <div class="form-group">
          <%= f.text_field :email, class:"form-control", placeholder:"Email address" %>
        </div>
        <div class="form-group">
          <%= f.text_area :message, class:"form-control", placeholder:"Send some message..." %>
        </div>
          <%= f.submit "Send invitation", class:"btn btn-sm btn-primary" %>
          <% end %>
        </div>
    </div>
</nav>