<!DOCTYPE html>
<html>

<head>
  <title>Smashello</title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
  <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
</head>

<body>
  <div class="wrapper">
    <div class="navbarDiv">
      <%= yield :navbar %>
      <%= yield :boardheader %>
    </div>

    <div class="parentDiv">
      <%= yield %>
    </div>
  </div>
</body>

</html>