<% content_for :navbar do %>
  <%= render 'shared/navbar' %>
<% end %>

<%= render "boardcreate", board: @board %>

<p class="board-notice"><%= notice %></p>

<div class="all-boards-container">
  <p class="board-title">
    <span class="fas fa-user-edit"></span>
    個人看板
  </p>

  <% @private_boards.each do |board| %>
    <ul class="personal-boards">
      <li>
        <%= link_to board_path(board.id), class: 'card-body' do %>
        <span class="card-title">
          <%= board.title %>
        </span>
        <% end %>
      </li>
    </ul>
  <% end %>
</div>
<div class="all-boards-container">
  <p class="board-title">
    <span class="fas fa-users"></span>
    團隊看板
  </p>
  <% @public_boards.each do |board| %>
    <ul class="team-boards">
      <li>
        <%= link_to board_path(board.id), class: 'card-body' do %>
        <span class="card-title">
          <%= board.title %>
        </span>
        <% end %>
      </li>
    </ul>
  <% end %>
</div>


<div class="card-create">
  <p class="board-create">
    <a href="" class="btn mb-4" data-toggle="modal" data-target="#createBoard">新增Board ＋</a>
  </p>
</div>

<div>
  <% @searchuser.each do |invitation| %>
  <h5>From user:<%= invitation.user_id %></h5>
  <p><%= invitation.message %></p>
  <p>Board ID:<%= invitation.board_id %></p>
  <p>invitation ID:<%= invitation.id %></p>
  <%= link_to "同意", agree_invite_boards_path(id: invitation.id, board_id: invitation.board_id, agree: true), method: "put", remote: true %>
  <%= link_to "不同意", agree_invite_boards_path(id: invitation.id, board_id: invitation.board_id, agree: false), method: "put", remote: true %>
  <% end %>
</div>
